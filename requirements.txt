# ============================================
# Architecture Migration Assistant
# Dependencies — CPU-only local stack
# ============================================

# --- Orchestration (LangGraph) ---
langgraph>=0.1.0
langchain-core>=0.2.0

# --- LLM & Embeddings (Local via Ollama) ---
ollama>=0.4.0

# --- RAG / Vector Store ---
chromadb>=0.5.0

# --- Code Analysis ---
radon>=6.0.0                # Cyclomatic complexity metrics
networkx>=3.0               # Dependency graph algorithms
python-louvain>=0.16        # Louvain community detection (community package)

# --- Code Generation & Formatting ---
Jinja2>=3.1.0               # Template rendering
black>=24.0.0               # Code formatting
isort>=5.13.0               # Import sorting

# --- Data Validation ---
pydantic>=2.0.0             # Schema validation for agent outputs
PyYAML>=6.0                 # Config file parsing

# --- Caching & Storage ---
diskcache>=5.6.0            # Local disk caching
# SQLite is built-in to Python

# --- Terminal UI ---
rich>=13.0.0                # DOS-style terminal dashboard

# --- Safety ---
# bandit>=1.7.0             # Security scanning (optional, install separately)

# --- Testing (for generated tests) ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
hypothesis>=6.0.0

# --- Web Framework (for sample monolith) ---
flask>=3.0.0

# --- Utilities ---
hashlib-additional>=1.0.0; python_version < "0"  # hashlib is stdlib, this is a no-op marker
